import { Stack } from 'expo-router';
 
// This layout defines the stack navigator for screens within the /player group.
export default function PlayerLayout() {
  // Configure stack options if needed
  return <Stack />;
} 