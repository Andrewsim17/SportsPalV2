import { Stack } from 'expo-router';
 
// This layout defines the stack navigator for screens within the /community group.
export default function CommunityLayout() {
  // Configure stack options if needed
  return <Stack />;
} 