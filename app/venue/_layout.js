import { Stack } from 'expo-router';

// This layout defines the stack navigator for screens within the /venue group.
export default function VenueLayout() {
  // You can configure the Stack navigator options here if needed
  // e.g., screenOptions={{ headerShown: false }}
  return <Stack />;
} 