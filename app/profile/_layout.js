import { Stack } from 'expo-router';
 
// This layout defines the stack navigator for screens within the /profile group.
export default function ProfileLayout() {
  // Configure stack options if needed
  return <Stack />;
} 