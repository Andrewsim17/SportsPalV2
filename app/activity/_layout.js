import { Stack } from 'expo-router';
 
// This layout defines the stack navigator for screens within the /activity group.
export default function ActivityLayout() {
  // Configure stack options if needed
  return <Stack />;
} 