import { Stack } from 'expo-router';
 
// This layout defines the stack navigator for screens within the /chat group.
export default function ChatLayout() {
  // Configure stack options if needed
  return <Stack />;
} 