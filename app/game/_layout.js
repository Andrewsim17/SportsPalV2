import { Stack } from 'expo-router';
 
// This layout defines the stack navigator for screens within the /game group.
export default function GameLayout() {
  // Configure stack options if needed
  return <Stack />;
} 